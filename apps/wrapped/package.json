{
  "name": "wrapped",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate:dev": "wrangler d1 migrations apply WRAPPED_DB --local",
    "db:migrate:prod": "wrangler d1 migrations apply WRAPPED_DB --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.6",
    "jose": "^6.1.2",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/types": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@repo/utils": "workspace:*",
    "drizzle-kit": "^0.31.6",
    "wrangler": "^4.47.0"
  }
}